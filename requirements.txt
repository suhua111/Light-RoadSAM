Light-RoadSAM/
│
├── checkpoints/             # 存放训练权重 
├── dataset/ 
|    └─ sequences/           # 存放数据集
├── data/                    # 数据处理模块
│   ├── dataset.py           # PyTorch Dataset 类的实现
│   └── transforms.py        # 数据增强 (Resize, Normalize, 随机翻转等)
├── eval_results/            # 可视化评估结果
├── models/                  # 模型定义
│   ├── light_roadsam.py     # 整体模型组装
│   ├── tiny_vit.py          # 轻量化图像编码器
│   ├── prompt_generator.py  # 自动提示生成头 (Bbox Head)
│   ├── adapter_fpn.py       # 多尺度适配器与特征金字塔
│   └── mask_decoder.py      # 修改后的 Mask 解码器
│
├── utils/
│   ├── loss.py              # 蒸馏损失 (MSE)、分割损失 (Dice+Focal)、检测损失
│   └── box_ops.py           # 从 Mask 提取 GT Box 的工具函数 (连通域分析)
│
├── eval_and_vis.py          # 模型评估 (mIoU, AP 等指标)
├── train_distill.py         # 阶段一：特征蒸馏训练脚本
├── train_pipeline.py        # 阶段二：整体 Pipeline (Encoder+Prompt+Decoder) 微调
├── evaluate.py              # 阶段一模型评估
└── requirements.txt